* Run unit test with:
uv run pytest -v

* Run code for result by giving in as parameters. For example Day 1, Part 2 with:
uv run runner.py day_1 p2

* Add -v, -vv for extra logs (DEBUG, TRACE)
uv run runner.py day_1 p2 -v

